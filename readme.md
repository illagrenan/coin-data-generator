```bash
$ python main.py -h
usage: main.py [-h] [-coin-count COIN_COUNT] [-amount-count AMOUNT_COUNT]
   [-max-amount MAX_AMOUNT]
```
   
```bash
optional arguments:
  -h, --helpshow this help message and exit
  -coin-count COIN_COUNT, -c COIN_COUNT
Kolik minci pouzit? Vychozi je nahodne cislo.
  -amount-count AMOUNT_COUNT, -a AMOUNT_COUNT
Kolik castek vygenerovat? Vychozi je nahodne cislo.
  -max-amount MAX_AMOUNT, -m MAX_AMOUNT
Maximalni pripustna castka. Vychozi je nahodne cislo.
```